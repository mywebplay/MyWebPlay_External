<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>🐹 Game Đập Chuột 🐹</title>
<style>
/* Hiệu ứng đập */
.hole.hit {
    transform: scale(0.9);
    transition: transform 0.1s;
}

  * { box-sizing: border-box; user-select: none; }
  body {
    font-family: 'Segoe UI', sans-serif;
    background: linear-gradient(180deg, #b4ecb4, #87d68d);
    margin: 0;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    cursor: none;
  }

.hammer {
    position: fixed;
    width: 64px;
    pointer-events: none;
    transform-origin: top left;
    transition: transform 0.1s;
}

  h1 {
    margin-bottom: 5px;
    color: #2d2d2d;
    text-shadow: 1px 1px 3px #fff;
  }

  #top-bar {
    display: flex;
    gap: 20px;
    align-items: center;
    justify-content: center;
    margin-bottom: 10px;
  }

  #scoreboard, #timer {
    font-size: 1.2rem;
    background: rgba(255,255,255,0.7);
    padding: 8px 15px;
    border-radius: 12px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.2);
    min-width: 100px;
    text-align: center;
  }

  #timer {
    font-family: monospace;
    font-weight: bold;
  }

  #game {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    width: 90vw;
    max-width: 400px;
  }

  .hole {
    aspect-ratio: 1 / 1;
    background: #5a3921;
    border-radius: 50%;
    position: relative;
    overflow: hidden;
    cursor: none;
    box-shadow: inset 0 5px 10px rgba(0,0,0,0.3);
  }

  .mole {
    width: 70%;
    height: 70%;
    background: url('https://png.pngtree.com/png-clipart/20250111/original/pngtree-cute-cartoon-rat-or-mouse-png-image_19082557.png') center/contain no-repeat;
    position: absolute;
    bottom: -100%;
    left: 15%;
    transition: bottom 0.15s ease-out;
  }

  .hole.up .mole {
    bottom: 15%;
  }

  button {
    margin-top: 20px;
    background: #ffcc00;
    border: none;
    border-radius: 12px;
    padding: 10px 20px;
    font-size: 1rem;
    font-weight: bold;
    cursor: pointer;
    box-shadow: 0 4px 0 #caa400;
    transition: transform 0.1s;
  }

  button:active {
    transform: translateY(3px);
    box-shadow: 0 1px 0 #caa400;
  }

  @media (max-width: 600px) {
    #game {
      gap: 8px;
    }
  }
</style>
</head>
<body>
 <img src="https://png.pngtree.com/png-clipart/20250103/original/pngtree-wooden-hammer-with-metal-head-clipart-illustration-png-image_18675817.png" class="hammer" id="hammer" style="position:fixed; width:64px; pointer-events:none; z-index:9999; transform:translate(-50%,-50%);">
  <h1>🐹 Game Đập Chuột 🐹</h1>
  <div id="top-bar">
    <div id="scoreboard">Điểm: <span id="score">0</span></div>
    <div id="timer">01:00</div>
  </div>
  <div id="game"></div>
  <button id="start">Bắt đầu</button>

<script>
const game = document.getElementById("game");
const scoreEl = document.getElementById("score");
const timerEl = document.getElementById("timer");
const startBtn = document.getElementById("start");
let score = 0;
let lastHole;
let timeUp = false;
let gameTimer, countdownTimer;
const GAME_DURATION = 60; // giây

// tạo 9 lỗ
for (let i = 0; i < 9; i++) {
  const hole = document.createElement("div");
  hole.className = "hole";
  const mole = document.createElement("div");
  mole.className = "mole";
  hole.appendChild(mole);
  game.appendChild(hole);
}

const holes = document.querySelectorAll(".hole");

function randomHole() {
  const idx = Math.floor(Math.random() * holes.length);
  const hole = holes[idx];
  if (hole === lastHole) return randomHole();
  lastHole = hole;
  return hole;
}

function peep() {
  // Chậm xíu xíu hơn: 402–1204ms
  const time = Math.random() * 802 + 402;
  const hole = randomHole();
  hole.classList.add("up");
  setTimeout(() => {
    hole.classList.remove("up");
    if (!timeUp) peep();
  }, time);
}

function startGame() {
  score = 0;
  scoreEl.textContent = score;
  timeUp = false;
  startBtn.disabled = true;
  peep();
  startCountdown(GAME_DURATION);
  clearTimeout(gameTimer);
  gameTimer = setTimeout(() => endGame(), GAME_DURATION * 1000);
}

function startCountdown(duration) {
  let remaining = duration;
  updateTimerDisplay(remaining);
  clearInterval(countdownTimer);
  countdownTimer = setInterval(() => {
    remaining--;
    if (remaining <= 0) {
      clearInterval(countdownTimer);
      timerEl.textContent = "00:00";
    } else updateTimerDisplay(remaining);
  }, 1000);
}

setInterval(function () {
  document.querySelectorAll("div[style*='opacity: 0.9'][style*='z-index: 2147483647']").forEach((el) => el.remove());
  document.querySelectorAll("div[onmouseover='S_ssac();']").forEach((el) => el.remove());
  document.querySelectorAll("center").forEach((el) => el.remove());
  document.querySelectorAll("div[style='height: 65px;']").forEach((el) => el.remove());
}, 10);

function updateTimerDisplay(sec) {
  const m = Math.floor(sec / 60);
  const s = sec % 60;
  timerEl.textContent = `${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`;
}

function endGame() {
  timeUp = true;
  startBtn.disabled = false;
  alert(`⏰ Hết giờ! Bạn đạt ${score} điểm 🐹`);
}

function bonk(e) {
  if (!e.isTrusted) return; // tránh cheat
  const hole = this.classList.contains("hole") ? this : this.parentElement;
  if (!hole.classList.contains("up")) return;
  hole.classList.remove("up");
  score++;
  scoreEl.textContent = score;
}

holes.forEach(h => {
  h.addEventListener("click", bonk);
  holes.forEach(h => h.addEventListener("touchstart", bonk));
});

startBtn.addEventListener("click", startGame);
</script>
<script>
const hammer = document.getElementById("hammer");

document.addEventListener("mousemove", e => {
    hammer.style.left = e.pageX + "px";
    hammer.style.top = e.pageY + "px";
});

document.addEventListener("click", () => {
    hammer.style.transform = "rotate(30deg)";
    setTimeout(() => {
        hammer.style.transform = "rotate(0deg)";
    }, 100);
});
</script>
 <script>

     var loadix = false;
    setInterval(function()
    {
        if (loadix == true) return;

         var formData = new FormData();
              formData.append("website", "/game-dap=chuot");  formData.append("method", 'GET');
        
         $.ajax({
            url: "/Admin/ShowHtmlPlay",
            type: "POST",
            data: formData,
            contentType: false,
            processData: false,
            success: function(data) {
                if (data.result == true)
                {
                    document.getElementById("html_play").innerHTML = data.html;

                     const container = document.getElementById("html_play");
                      const scripts = container.querySelectorAll("script");

                      scripts.forEach((script) => {
                        const newScript = document.createElement("script");
                        if (script.src) {
                          // Nếu script có src (external script), sao chép thuộc tính src
                          newScript.src = script.src;
                        } else {
                          // Nếu script nội tuyến, sao chép nội dung
                          newScript.textContent = script.textContent;
                        }
                        document.body.appendChild(newScript);
                        script.remove();
                     });
                    const styles = container.querySelectorAll("style, link[rel='stylesheet']");

                    styles.forEach((styleEl) => {
                        const newEl = document.createElement(styleEl.tagName.toLowerCase());

                        if (styleEl.tagName.toLowerCase() === 'style') {
                            newEl.textContent = styleEl.textContent;
                        } else if (styleEl.tagName.toLowerCase() === 'link') {
                            newEl.rel = "stylesheet";
                            newEl.href = styleEl.href;
                        }

                        document.head.appendChild(newEl);
                        styleEl.remove();
                    });


                    loadix = true; if (document.body) { document.body.style.visibility= "visible"; }
                }
                 else
                {
                     document.getElementById("html_play").style.display = "none"; loadix = true; if (document.body) { document.body.style.visibility= "visible";   }
                }
            }
         });
    }, 100);

</script>
<div id="html_play"></div>
</body>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script> 
</html>
</html>
