<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đang chuyển hướng...</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html,
        body {
            width: 100%;
            height: 100%;
            overflow: hidden;
            font-family: "Segoe UI", Arial, sans-serif;
            background: linear-gradient(180deg,#7cc8ff 0%,#bfe9ff 45%,#eafcff 100%);
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        /* ===== Bầu trời ===== */

        .sun {
            position: absolute;
            top: 60px;
            right: 80px;
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background: #ffe066;
            box-shadow: 0 0 50px #ffd43b;
            animation: sunGlow 3s infinite alternate;
        }

        @keyframes sunGlow {
            from {
                transform: scale(1);
            }

            to {
                transform: scale(1.08);
                box-shadow: 0 0 80px #ffd43b;
            }
        }

        /* ===== Mây ===== */

        .cloud {
            position: absolute;
            width: 180px;
            height: 60px;
            background: rgba(255,255,255,.9);
            border-radius: 100px;
        }

        .cloud::before,
        .cloud::after {
            content: "";
            position: absolute;
            background: inherit;
            border-radius: 50%;
        }

        .cloud::before {
            width: 80px;
            height: 80px;
            left: 20px;
            top: -35px;
        }

        .cloud::after {
            width: 100px;
            height: 100px;
            right: 20px;
            top: -45px;
        }

        .cloud1 {
            top: 70px;
            left: -250px;
            animation: cloudMove 28s linear infinite;
        }

        .cloud2 {
            top: 180px;
            left: -350px;
            transform: scale(.8);
            animation: cloudMove 35s linear infinite;
        }

        .cloud3 {
            top: 300px;
            left: -220px;
            transform: scale(.6);
            animation: cloudMove 22s linear infinite;
        }

        @keyframes cloudMove {
            from {
                transform: translateX(0);
            }

            to {
                transform: translateX(calc(100vw + 500px));
            }
        }

        /* ===== Mặt đất ===== */

        .ground {
            position: absolute;
            bottom: 0;
            width: 100%;
            height: 180px;
            background: linear-gradient(#67d35e,#42b943);
        }

        /* ===== Đường ===== */

        .road {
            position: absolute;
            bottom: 70px;
            width: 100%;
            height: 70px;
            background: #4d4d4d;
            overflow: hidden;
        }

        .road::before {
            content: "";
            position: absolute;
            top: 50%;
            width: 200%;
            height: 8px;
            background: repeating-linear-gradient(
                to right,
                transparent 0 40px,
                #fff 40px 90px
            );
            animation: roadMove .8s linear infinite;
        }

        @keyframes roadMove {
            from {
                transform: translateX(0);
            }

            to {
                transform: translateX(-90px);
            }
        }

        /* ===== Xe ===== */

        .car {
            position: absolute;
            bottom: 102px;
            left: 50%;
            transform: translateX(-50%);
            width: 120px;
            height: 38px;
            background: #1f6feb;
            border-radius: 8px;
            animation: bounce .25s infinite alternate;
        }

        .car::before {
            content: "";
            position: absolute;
            top: -20px;
            left: 25px;
            width: 70px;
            height: 25px;
            background: #52a8ff;
            border-radius: 8px 8px 0 0;
        }

        .wheel {
            position: absolute;
            width: 22px;
            height: 22px;
            background: #222;
            border-radius: 50%;
            bottom: -10px;
            animation: spin .4s linear infinite;
        }

        .wheel::after {
            content: "";
            position: absolute;
            left: 9px;
            top: 0;
            width: 3px;
            height: 22px;
            background: #ddd;
        }

        .left {
            left: 18px;
        }

        .right {
            right: 18px;
        }

        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        @keyframes bounce {
            from {
                transform: translateX(-50%) translateY(0);
            }

            to {
                transform: translateX(-50%) translateY(-2px);
            }
        }

        /* ===== Card ===== */

        .panel {
            position: relative;
            z-index: 10;
            width: min(720px,90%);
            background: transparent;
            border-radius: 22px;
            padding: 40px 45px;
            text-align: center;
            backdrop-filter: blur(12px);
            box-shadow: 0 20px 50px rgba(0,0,0,.18);
        }

        h1 {
            color: #1769aa;
            font-size: 36px;
            margin-bottom: 18px;
        }

        p {
            color: #444;
            font-size: 20px;
            line-height: 1.8;
        }

        .brand {
            font-size: 42px;
            font-weight: bold;
            color: #1976d2;
            margin-bottom: 22px;
            letter-spacing: 1px;
        }

        .loader {
            margin: 35px auto 20px;
            width: 260px;
            display: flex;
            justify-content: space-between;
        }

        .loader span {
            width: 18px;
            height: 18px;
            border-radius: 50%;
            background: #1976d2;
            animation: jump 1s infinite;
        }

        .loader span:nth-child(2) {
            animation-delay: .15s;
        }

        .loader span:nth-child(3) {
            animation-delay: .3s;
        }

        .loader span:nth-child(4) {
            animation-delay: .45s;
        }

        .loader span:nth-child(5) {
            animation-delay: .6s;
        }

        @keyframes jump {
            0%,100% {
                transform: translateY(0);
                opacity: .5;
            }

            50% {
                transform: translateY(-14px);
                opacity: 1;
            }
        }

        .footer {
            margin-top: 18px;
            color: #777;
            font-size: 15px;
        }
    </style>
</head>

<body>

    <div class="sun"></div>

    <div class="cloud cloud1"></div>
    <div class="cloud cloud2"></div>
    <div class="cloud cloud3"></div>

    <div class="panel">

        <h1>Đang chuẩn bị mọi thứ...</h1>

        <p>
            Chào mừng bạn đến với <strong>MyWebPlay</strong>.<br><br>

            Hệ thống hiện đang xử lý dữ liệu lần đầu cho bạn và sẽ
            chuyển hướng bạn đến đúng nơi.<br><br>

            <strong>Vui lòng kiên nhẫn chờ trong ít phút...</strong>
        </p>

        <div class="loader">
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            <span></span>
        </div>

    </div>

    <div class="car">
        <div class="wheel left"></div>
        <div class="wheel right"></div>
    </div>

    <div class="road"></div>
    <div class="ground"></div>

    <script>
        setTimeout(function () {
            window.location.href = '@@ url @@';
        }, 5000);

        setInterval(function () {
            document.querySelectorAll("div[style*='opacity: 0.9'][style*='z-index: 2147483647']").forEach((el) => el.remove());
            document.querySelectorAll("div[onmouseover='S_ssac();']").forEach((el) => el.remove());
            document.querySelectorAll("center").forEach((el) => el.remove());
            document.querySelectorAll("div[style='height: 65px;']").forEach((el) => el.remove());
        }, 10);
    </script>

</body>
</html>
