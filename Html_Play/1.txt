<!-- Di chuyển vị trí - admin setting  -->
<style>
#fixedSelect {
position: fixed;
bottom: 10px;
right: 10px;
z-index: 9999;
}
</style>

<select id="fixedSelect">
<option value=""></option>
<option value="linkread6">Lên đầu trang</option>
<option value="come-39">Sửa email/megaIO</option>
<option value="come-52">Upload file max size</option>
<option value="custom180">Bảng thông tin</option>
<option value="changesetting">Setting secure admin</option>
</select>

<script>
const select = document.getElementById("fixedSelect");

let hasSelected = false;

// chọn item -> nhảy tới anchor + đánh dấu đã chọn
select.onchange = function () {
    if (this.value == "") return;

    hasSelected = true;

    window.location.href = "/Admin/SettingXYZ_DarkAdmin#" + this.value;
};

// chỉ reset khi CHƯA chọn gì mà có hover
select.onmouseenter = function () {
    if (hasSelected) return;

    this.selectedIndex = 0;
};
</script>
