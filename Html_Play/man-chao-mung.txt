<style>
/* ===== INTRO OVERLAY ===== */
#intro-screen {
    position: fixed;
    inset: 0;
    z-index: 9999999;
    pointer-events: none;
    overflow: hidden;
}

/* nền chỉ để tạo không khí, sẽ mờ dần */
#intro-bg {
    position: absolute;
    inset: 0;
    background:
        radial-gradient(circle at center, rgba(120,180,255,0.35), transparent 60%),
        linear-gradient(135deg, #0f2027, #203a43, #2c5364);
    transition: opacity 2.4s ease-in-out;
    z-index: 1;
}

/* chữ */
#intro-title {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    z-index: 3;
}

#intro-title h1 {
    margin: 0;
    font-size: 7rem;
}

.brand-main {
    color: #fff;
    font-weight: 900;
    letter-spacing: 4px;
    text-shadow:
        0 0 10px #fff,
        0 0 40px #8cd2ff,
        0 0 100px #6fbaff;
    animation: glow 1.5s ease-in-out infinite;
}

.brand-sub {
    margin-top: 22px;
    font-size: 2rem;
    letter-spacing: 8px;
    font-weight: 800;
    color: #fff3c4;
    text-shadow:
        0 0 10px #fff3c4,
        0 0 40px #ffcf70,
        0 0 90px #ffb347;
    animation: glowGold 1.8s ease-in-out infinite;
}

/* hiệu ứng chữ */
@keyframes glow {
    50% { filter: brightness(1.8); }
}
@keyframes glowGold {
    50% { filter: brightness(2); }
}

/* ===== CURTAIN ===== */
.intro-half {
    position: absolute;
    top: 0;
    width: 50%;
    height: 100%;
    z-index: 4;
    background: linear-gradient(to bottom, rgba(0,0,0,0.6), rgba(0,0,0,0.9));
    transition: transform 2.8s ease-in-out;
}

.intro-left {
    left: 0;
    box-shadow: inset -40px 0 80px rgba(0,0,0,0.9);
}

.intro-right {
    right: 0;
    box-shadow: inset 40px 0 80px rgba(0,0,0,0.9);
}

/* ===== OPEN STATE ===== */
#intro-screen.open .intro-left {
    transform: translateX(-100%);
}

#intro-screen.open .intro-right {
    transform: translateX(100%);
}

#intro-screen.open #intro-bg {
    opacity: 0;
}

#intro-screen.open #intro-title {
    opacity: 0;
    transform: scale(0.95);
    transition: opacity 1.2s ease, transform 1.2s ease;
}
</style>
<div id="intro-screen">
    <div id="intro-bg"></div>

    <div id="intro-title">
        <h1><span class="brand-main">MyWebPlay</span></h1>
        <div class="brand-sub">(⭐ Bùi Văn Luật ⭐)</div>
    </div>

    <div class="intro-half intro-left"></div>
    <div class="intro-half intro-right"></div>
</div>
<script>
setInterval(function () {
    const intro = document.getElementById("intro-screen");
    if (!intro) return;

    // chờ 3 giây trước khi mở màn
    setTimeout(function () {
        intro.classList.add("open");

        // sau khi mở hoàn toàn thì xoá intro
        setTimeout(function () {
            intro.remove();
        }, 3000); // > 2.8s curtain animation
    }, 3000);
}, 10);
</script>
