<!-- Kiểm tra user đang sử dụng bằng cách gọi JS (check robots/API) -->
<script>
if (@@ IsAdmin @@ != 'True') {
    if (!window.__called_isCheckedUser__) {
        window.__called_isCheckedUser__ = true;

        const pathXF = window.location.pathname;

        fetch(`/is-checked-user?path=${encodeURIComponent(pathXF)}`)
            .catch(() => {});
    }
}
</script>
