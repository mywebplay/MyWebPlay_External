<style>
    body {
    height: 2000px;
    font-family: Arial, sans-serif;
    font-size:30px;
}

#noteModal {
    position: fixed;
    bottom: 20px;
    right: 20px;

    background: #fff;
    border: 1px solid #ccc;
    border-radius: 10px;
    padding: 15px 20px;
    box-shadow: 0 4px 10px rgba(0,0,0,0.2);

    z-index: 9999;
    width: max-content;

    /* 🔥 FIX QUAN TRỌNG */
    will-change: transform;
    backface-visibility: hidden;
}

    #closeBtn {
        position: absolute;
        top: -35px;
        right: -20px;
        cursor: pointer;
        font-weight: bold;
    }

    #noteModal h3 {
        color: red;
        margin: 0 0 10px 0;
    }

    /* mỗi dòng */
    .note-row {
        display: flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 6px;
        cursor: pointer;
    }

    /* ô vuông màu */
    .color-box {
        width: 14px;
        height: 14px;
        border-radius: 3px;
        flex-shrink: 0;
    }

    .fixed-box {
        background: burlywood;
    }

    .closed-box {
        background: darkgrey;
    }

    .nobug-box {
            background: chartreuse;
        }

    .pending-box {
        background: cornflowerblue;
    }

    .trick-box {
        background: darkgoldenrod;
    }

    /* chữ */
    .label {
        font-weight: bold;
    }

</style>
<div id="noteModal">
    <span id="closeBtn">✖</span>

    <h3>GHI CHÚ</h3>

    <div class="note-row" onclick="showFixed()">
        <div class="color-box fixed-box"></div>
        <div class="label">FIXED</div>
    </div>

<div class="note-row" onclick="showNoBug()">
        <div class="color-box nobug-box"></div>
        <div class="label">NO BUG</div>
    </div>

    <div class="note-row" onclick="showClosed()">
        <div class="color-box closed-box"></div>
        <div class="label">CLOSED</div>
    </div>

    <div class="note-row" onclick="showPending()">
        <div class="color-box pending-box"></div>
        <div class="label">PENDING</div>
    </div>

    <div class="note-row" onclick="showTrick()">
        <div class="color-box trick-box"></div>
        <div class="label">TRICK</div>
    </div>

</div>

<script>
    // Close modal (chỉ tắt trong request hiện tại)
    document.getElementById("closeBtn").onclick = function () {
        document.getElementById("noteModal").style.display = "none";
    };

    function showFixed() {
        alert("Đây là loại bug đã được fix, đã được test xác nhận là hết lỗi xảy ra!");
    }

   function showClosed() {
        alert("Bug này đã/tạm thời bị đóng lại hoặc chờ xác nhận sau!");
    }

    function showPending() {
        alert("Đây là loại bug chưa được fix (hoặc đã được fix nhưng chưa hiểu vì sao, vô tình hay cố ý), nhưng hiện tại đã không còn tái hiện được hoặc đang chờ xác nhận!");
    }

    function showTrick() {
        alert("Đề xuất một chức năng mới, mưu mẹo/cách gian lận để bỏ qua bug hoặc loại bỏ tính năng liên quan đến bug được đề cập.\n(fix bug tạm thời)");
    }

 function showNoBug() {
        alert("Xác nhận đây không phải là bug hoặc là do setting sai.");
    }
</script>
<script>
  function DanhDauFixed(indes)
  {
      for (var i = 0; i < indes.length; i++)
      {
        document.getElementById("tr_"+ indes[i]).style.backgroundColor = "burlywood";
      }
  }

  function DanhDauPending(indes)
  {
      for (var i = 0; i < indes.length; i++)
      {
        document.getElementById("tr_"+ indes[i]).style.backgroundColor = "cornflowerblue";
      }
  }

  function DanhDauTrick(indes)
  {
      for (var i = 0; i < indes.length; i++)
      {
        document.getElementById("tr_"+ indes[i]).style.backgroundColor = "darkgoldenrod";
      }
  }

 function DanhDauNoBug(indes)
  {
      for (var i = 0; i < indes.length; i++)
      {
        document.getElementById("tr_"+ indes[i]).style.backgroundColor = "chartreuse";
      }
  }

 function DanhDauClosed(indes)
  {
      for (var i = 0; i < indes.length; i++)
      {
        document.getElementById("tr_"+ indes[i]).style.backgroundColor = "darkgrey";
      }
  }

  DanhDauFixed([1,2,4,6,10,13,24,28,33,34,35,36,37,38,39,41,42]);
  DanhDauPending([7,8,14,20]);
  DanhDauTrick([3,5,9,18,30,31]);
  DanhDauNoBug([25,40]);
  DanhDauClosed([22]);

//---------------------------------------

document.getElementById("dlCsv").disabled = true;

</script>
