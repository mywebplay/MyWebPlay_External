<style>
    body {
        height: 2000px;
        font-family: Arial, sans-serif;
    }

    #noteModal {
        position: fixed;
        bottom: 20px;
        right: 20px;
        background: #fff;
        border: 1px solid #ccc;
        border-radius: 10px;
        padding: 15px 20px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        z-index: 9999;
        width: max-content;
    }

    #closeBtn {
        position: absolute;
        top: 5px;
        right: 10px;
        cursor: pointer;
        font-weight: bold;
    }

    #noteModal h3 {
        color: red;
        margin: 0 0 10px 0;
    }

    /* mỗi dòng */
    .note-row {
        display: flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 6px;
        cursor: pointer;
    }

    /* ô vuông màu */
    .color-box {
        width: 14px;
        height: 14px;
        border-radius: 3px;
        flex-shrink: 0;
    }

    .closed-box {
        background: burlywood;
    }

    .pending-box {
        background: cornflowerblue;
    }

    .trick-box {
        background: darkgoldenrod;
    }

    /* chữ */
    .label {
        font-weight: bold;
    }

</style>
<div id="noteModal">
    <span id="closeBtn">✖</span>

    <h3>GHI CHÚ</h3>

    <div class="note-row" onclick="showClosed()">
        <div class="color-box closed-box"></div>
        <div class="label">CLOSED</div>
    </div>

    <div class="note-row" onclick="showPending()">
        <div class="color-box pending-box"></div>
        <div class="label">PENDING</div>
    </div>

    <div class="note-row" onclick="showTrick()">
        <div class="color-box trick-box"></div>
        <div class="label">TRICK</div>
    </div>

</div>

<script>
    // Close modal (chỉ tắt trong request hiện tại)
    document.getElementById("closeBtn").onclick = function () {
        document.getElementById("noteModal").style.display = "none";
    };

    function showClosed() {
        alert("Đây là loại bug đã được fix, đã được test xác nhận là hết lỗi xảy ra!");
    }

    function showPending() {
        alert("Đây là loại bug chưa được fix (hoặc đã được fix nhưng chưa hiểu vì sao, vô tình hay cố ý), nhưng hiện tại đã không còn tái hiện được hoặc đang chờ xác nhận!");
    }

    function showTrick() {
        alert("Đề xuất một chức năng mới, mưu mẹo/cách gian lận để bỏ qua bug hoặc loại bỏ tính năng liên quan đến bug được đề cập (fix bug tạm thời)!");
    }
</script>
<script>
  function DanhDauClose(indes)
  {
      for (var i = 0; i < indes.length; i++)
      {
        document.getElementById("tr_"+ indes[i]).style.backgroundColor = "burlywood";
      }
  }

  function DanhDauPending(indes)
  {
      for (var i = 0; i < indes.length; i++)
      {
        document.getElementById("tr_"+ indes[i]).style.backgroundColor = "cornflowerblue";
      }
  }

  function DanhDauTrick(indes)
  {
      for (var i = 0; i < indes.length; i++)
      {
        document.getElementById("tr_"+ indes[i]).style.backgroundColor = "darkgoldenrod";
      }
  }

  DanhDauClose([1,6,13]);
  DanhDauPending([8,14,20]);
  DanhDauTrick([3,5,9,18]);

//---------------------------------------

document.getElementById("dlCsv").disabled = true;

</script>
